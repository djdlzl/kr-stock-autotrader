Telegram `mac` thread 7923 Giraffe 07:00 paper-only research. Keep `LIVE_TRADING=false`; do not create orders, capital allocations, user decisions, decision cards, Paperclip, JWC, or cross-topic work.

The prehook owns full DART capture, the immutable control contract, and run registration. Its successful `GIRAFFE_DART_GATE_V1` output is intentionally compact. Require `complete=true`, `compaction_required=true`, and use only `run_key`, `control_contract_sha256`, `control_count`, `source_valid_count`, and `source_error_count`. The counts must add exactly; otherwise do no research/search/storage and finish error. `control_contract_sha256` is canonical JSON of parsed `control_contract` (UTF-8, `ensure_ascii=False`, `sort_keys=True`, `separators=(',', ':')`), never the raw artifact hash.

Do not request or inject a full contract/source array. Start with `python -m kr_stock_autotrader.cli scheduler-start "$run_key" research`; page `giraffe-review-queue "$run_key" "$control_contract_sha256" --offset 0 --limit 25`; preserve exact returned order/identity; open `giraffe-review-packet "$run_key" "$control_contract_sha256" "$rcp_no"` only for semantic review. The CLI validates the closed wire shape and packet provenance. A malformed response, hash/order/count mismatch, or duplicate/missing receipt is fail-closed.

Follow `prompts/giraffe-material-discovery-v1.md` as the compact operator procedure. It does not duplicate API schemas: use the existing bounded terminal batch, evidence readback, finish, and exact scheduler-readback APIs unchanged. Never synthesize timestamps or reject potentially material non-supply reports by title alone.
